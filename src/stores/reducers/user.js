import * as types from '../types'

const initialState = {
  userName: '',
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
